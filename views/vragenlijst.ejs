<%- include('./partials/head') %>
<nav class="navbar">
  <div class="navbar-item">
    <form action="/" method="post">
      <button class="back-button">Terug naar Inloggen</button>
    </form>
  </div>
  <div class="navbar-brand">
    <span class="navbar-item">U bent nu ingelogd als: Bedrijf X</span>
  </div>
  <div class="navbar-menu">
    <div class="navbar-end">
    </div>
  </div>
</nav>
<div class="vragenlijst">
  <h1>Kan je per SDG op een schaal van 1/5 aangeven hoe groot de <span>positieve</span> bijdrage is? 3/4</h1>
  <h2>Denk hier heel zorgvuldig over na, dit is belangrijk voor de uiteindelijke uitslag.</h2>
  <% if (data && data.length > 0) { %>
    <% clickedImages.forEach(index => { %>
      <% const currentItem = data[index]; %>
      <section class="chosen-items">
        <div class="item">
          <img src="https://fdnd-agency.directus.app/assets/<%= currentItem.icon %>" width="100" class="selected-img">
          <h3 class="current-item"><%= currentItem.description %></h3>
          <input type="number" name="scores[]" id="score_<%= currentItem.id %>" min="-5" max="5" required>
        </div>
      </section>
    <% }); %>
  <% } else { %>
    <p>No data available</p>
  <% } %>
  <div class="vragenlijst-buttons">
    <form action="/SDG" method="post">
      <button class="back-button">Back</button>
    </form>
    <form action="/dashboard" method="post">
      <button type="submit" class="vragenlijst-btn">Submit</button>
    </form>
    </div>
</div>

<%- include('./partials/foot') %>
